{
  "name": "lzway-3.5",
  "version": "3.5.0-dev",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-structure.mjs && node scripts/check-architecture.mjs && node scripts/check-release.mjs",
    "test:domain": "node --test tests/*.test.mjs",
    "test": "npm run check && npm run test:domain",
    "prepare:android": "rm -rf www && mkdir -p www && cp index.html sw.js www/ && cp -R src public www/",
    "build:site": "rm -rf dist && mkdir -p dist && cp index.html sw.js dist/ && cp -R src public dist/"
  },
  "dependencies": {
    "@capacitor/android": "6.2.0",
    "@capacitor/core": "6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "6.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
