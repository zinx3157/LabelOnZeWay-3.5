{
  "name": "labelonzeway-3.5",
  "version": "3.5.0-dev",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-structure.mjs && node scripts/check-architecture.mjs",
    "test:domain": "node --test tests/*.test.mjs",
    "test": "npm run check && npm run test:domain"
  },
  "engines": {
    "node": ">=20"
  }
}
